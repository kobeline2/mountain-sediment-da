# Architecture

このディレクトリには、プロジェクト全体の構成方針と、
各サブシステムの責務分離に関する文書を置きます。

現在の基本方針は次の通りです。

- `hydrophone-gp/` は観測から流砂量を推定するローカル解析系
- `simhis-external/` は外部の `simhis` 実行者と受け渡すための境界面
- `thesis/` は論文・図表・記述メモの整理領域
- `legacy/` は旧作業ディレクトリの退避領域
