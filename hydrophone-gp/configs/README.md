# Configs

このディレクトリには、`hydrophone-gp` の実行設定を置きます。

- `local_paths.sample.m`
  - マシン依存の外部依存設定
- `parameters.sample.m`
  - 学習/予測の実行設定サンプル

実際に使うファイルは以下です。

- `local_paths.m`
- `parameters.m`

これらは Git 追跡対象から外しています。
未作成でも、`*.sample.m` を使って実行できます。
